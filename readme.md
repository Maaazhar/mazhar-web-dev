# Redirect any FireBase website to any URL in default.
## Write the following `code` in the `firebase.json` file
`"redirects": [
      {
        "source": "/",
        "destination": "destination.url",
        "type": 301
      }
    ]`


